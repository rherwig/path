const cwd = require('./functions/cwd');
const endsWith = require('./functions/ends-with');
const relative = require('./functions/relative');

module.exports = {
    cwd,
    endsWith,
    relative,
};
